interface:
  display_name: "karta-status"
  short_description: "Show what's next in a karta run — where you are and the single next action — derived fresh from git, read-only."
policy:
  allow_implicit_invocation: true
