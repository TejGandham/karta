interface:
  display_name: "karta-plan"
  short_description: "Synthesize a validated binder of work items from a problem, feature, or design — stack-agnostic."
policy:
  allow_implicit_invocation: true
