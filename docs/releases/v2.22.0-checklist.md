# karta 2.22.0 — release test-drive checklist

Karta 2.22.0 releases the **built-in Go stack packs**: `go-naming` and `go-htmx` join `minimalism` and the other built-ins under `skills/_shared/sme/`, pinnable per binder via `sme[]` and seedable into a consumer repo's `.karta/sme/`. The packs themselves landed on main 2026-07-15 (`5984580`, review fixes `4fbd308`) — eighteen minutes *after* the 2.21.0 bump — and sat unreleased for six days at the wrong version. That gap was caught during release verification of unrelated bench work, which is exactly the class of drift the bench's field-adoption lane exists to surface; this release closes it.

This is also the **first release gated by the framework bench**: the version-bump commit must ship a green full-gate results file stamped with the new version and the RC sha, enforced by `scripts/hooks/precommit_gate.py` (release block), or the commit is refused.

## Pre-flight (done for 2.22.0)

- [x] Version is `2.22.0` in all four plugin manifests; parity guarded by `validate_plugin.py` (PASS on the bumped tree).
- [x] Green full-gate file at the RC sha staged with the bump (`benchmarks/results/gate/2026-07-21-v2.22.0-gate.json` — version-qualified name so the same-day 2.21.0 baseline artifact `2026-07-21-gate.json` is preserved, not clobbered).
- [x] `feature-manifest.json` entry `builtin-go-packs` added (detection: grep `go-naming` under `.karta`, durable) with `ship_date` equal to this release commit's committer date; coverage-floor note advanced to 2.22.0. Manifest gate self-test 15/15.
- [x] Pack content already field-exercised: gringotts seeds both packs in `.karta/sme/` and pins them in binders (pre-release dogfood; the adoption ledger will label the negative lag accordingly).

## Test-drive (run with the 2.22.0 plugin installed)

### 1. Pack pinning

- [x] In a Go consumer repo (gringotts), plan a binder with `"sme": ["go-naming", "go-htmx"]`; confirm the plan-time Review checklist surfaces both packs' rules. *(2026-07-21: plan derived `[minimalism, go-naming, go-htmx]`; the drafted card cited `htmx.1-8`, `goname.1/2/6/8/9`, `min.2/4` verbatim; gringotts left byte-untouched, commit never spoken. Caveat: rules surfaced from gringotts' `.karta/sme/` overlay — fingerprint `htmx.8`, an overlay-only rule — because the installed plugin cache was still 2.21.0, which carries no Go built-ins; the built-in auto-match path (exact-dep token `github.com/a-h/templ`, verified against `detect_stack.py` output) first fires in a session with the 2.22.0 cache.)*
- [ ] Build one item and confirm the build-time authoritative review cites pack rule ids, not paraphrases. *(2026-07-21: deferred to the next real gringotts delivery — a live build mutates the consumer repo. Mechanical half done: `validate_packs.py` OK on both packs.)*

### 2. Seeding and drift

- [x] With kaizen enabled, confirm first-run seeding copies both packs into `.karta/sme/` byte-identical to the built-ins. *(2026-07-21: real `karta-kaizen` agent, Mode: direct dispatch, on a fresh go+templ fixture — seeded `minimalism`, `go-naming`, `go-htmx`; all three `cmp`-verified byte-identical to built-ins; `validate_packs.py` 3/3 OK; writer confinement held, nothing written outside `.karta/sme/`.)*
- [x] `sme-pack-static-suite` probe: the known `seed-drift:gringotts:go-htmx:DIVERGENT` finding either persists (gringotts' local edits are real) or resolves after a reseed — either way the probe stays green and records it. *(2026-07-21: probe pass; drift persists and is recorded — `go-htmx` and `go-naming` both DIVERGENT, plus the overlay's overbroad/dead token findings. Field note: the overlay is ahead of the built-in on rules — kaizen-added `htmx.8` is an upstream candidate — and behind on match tokens, which 2.22.0 fixed to exact-dep matching.)*

### 3. Adoption ledger

- [x] Next `field-feature-adoption-ledger` run shows `builtin-go-packs` ADOPTED for gringotts with a `pre-release-dogfood` lag label, and AVAILABLE-BUT-UNUSED or NOT-EXERCISED-SINCE-SHIP for parchmark (no Go there — expected). *(2026-07-21: gringotts ADOPTED, first use `f9043a5` @ 2026-07-15T11:07 — 26 minutes before the packs landed on karta main — lag −6 days, label `pre-release-dogfood`; parchmark NOT-EXERCISED-SINCE-SHIP.)*
