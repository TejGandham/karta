interface:
  display_name: "karta-doc-gardner"
  short_description: "Automatically correct documentation drift to match the code (opt-in via .karta/doc-gardner.json)."
policy:
  allow_implicit_invocation: true
